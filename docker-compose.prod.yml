services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: duevora-app-prod
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env
    environment:
      - PORT=3000
      - NODE_ENV=production


