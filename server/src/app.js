// Importing modules
import express from "express";

// function to make the app 
function createApp() {

    // create an express app
    const app = express();

    // returning the app
    return app;

}

export default createApp;